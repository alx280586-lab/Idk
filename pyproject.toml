[project]
name = "nfce"
version = "0.1.0"
description = "Neural-Field Cognitive Engine skeleton"
authors = [{name = "NFCE"}]
requires-python = ">=3.11"
dependencies = [
    "pydantic",
    "torch",
]

[tool.setuptools]
package-dir = {"" = "src"}
